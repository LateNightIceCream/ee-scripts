# Regulator Output Tolerance
Gets the resistors from E-Series that minimize the output error for some given
output voltage and maximum error.

## Dependencies:
- `eseries`

```
python -m pip install eseries
```
